# Project 3 fixtures — region/interval stats

- `ref.fasta` — small reference with Ns
- `intervals.bed` — named intervals across contigs
- `intervals.gff3` — same regions represented as features (optional)
